<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- マイク入力(コード解析)に必須 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- WebViewのgetUserMedia(ChromiumのWebRTC音声キャプチャ)はRECORD_AUDIOに加えて
         これも要求する。無いと「No audio device available」→ NotReadableError になる。
         通常権限なのでインストール時に自動付与される(ダイアログは出ない)。 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 127.0.0.1で自前配信するローカルサーバーへWebViewから接続するために必要
         (ループバック通信でもAndroidのソケットAPIはINTERNET権限を要求する) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ChordDetector">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ChordDetector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
